import React from 'react';
import data from 'data.json';
class App extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      isClicked: false,
      name: '',
    };
  }

  render (){
    let movieDetails;
    if (this.state.name !== "")
  }
}
export default App;
